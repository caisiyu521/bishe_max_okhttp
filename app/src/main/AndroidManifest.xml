<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.caisiyu.bishe_max">

    <uses-permission android:name="android.permission.INTERNET"/>               //打开网络权限.
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>   //获取网络连接状态

    <application
        android:allowBackup="true"
        android:icon="@drawable/cangku"
        android:label="仓库管理系统"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity_Two"></activity>
    </application>

</manifest>
    <!--<activity android:name=".MainActivity_Two" />-->
